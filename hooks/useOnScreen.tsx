// import { KeyObject } from "crypto";
// import React, { useEffect, useState } from "react"

// const { height, width } = window.screen;
// const referencedElements: Map<React.RefObject<HTMLElement>, boolean> = new Map();

// window.addEventListener('wheel', (ev) => {
//     referencedElements.forEach((value, ref, map) => map.set(ref, ref.current!.offsetTop === 0));
// });

// export default function useIsOnBody(ref: React.RefObject<HTMLElement>): boolean {
//     if (!ref.current) {
//         return false;
//     }

//     if (!referencedElements.has(ref)) {
//         referencedElements.set(ref, false);
//     }

//     return referencedElements.get(ref)!.valueOf();
// }

export { };